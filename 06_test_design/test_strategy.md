# 测试策略

层级：单元、集成、系统、回归、性能、可靠性、故障注入、真实环境、用户验收。

ETC 验证：存在已批准的变化场景或可变更性 Requirement 时，加入最小充分的替换性测试、兼容性测试、稳定核心回归和回退验证；没有正式来源时不得因 ETC 自动扩大组合范围。

质量工具：编译器警告、静态分析、ASan、UBSan、必要时 TSan、Coverage。

每个真实 Bug 修复后原则上增加回归测试。
